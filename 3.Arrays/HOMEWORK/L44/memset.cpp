#include <bits/stdc++.h>
#include<stdio.h>
using namespace std;

int main() {
    // char str[5];
    // memset(str, 't', sizeof(str));
    // cout << str;

    // char arr[10]=;
    // arr[8]=6;
    
    // memset(arr, '3',8);
    // for(auto i: arr){
    //     cout<<i;
    // }

    int arr[]={1,3,5,7,9};

    memset(arr, '1',4);
    for(auto i:arr){
        cout<<i;
    }


    return 0;
}